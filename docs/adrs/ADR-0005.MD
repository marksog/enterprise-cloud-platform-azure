Public GitHub runners are not trusted to access the Terraform backend in the long term.

Temporary workaround:

* Storage firewall = Allow

Target architecture:

* Storage firewall = Deny
* Private Endpoint
* Self-hosted runner inside Azure VNet